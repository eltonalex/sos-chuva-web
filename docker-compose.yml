services:
  web:
    image: python:3.12-alpine
    container_name: sos-chuva-web
    working_dir: /srv
    command: python3 -m http.server 8000 --bind 0.0.0.0 --directory /srv
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./:/srv:ro
    restart: unless-stopped
